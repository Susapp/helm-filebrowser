apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.service.name}}-custom-css
  labels:
    app: {{ .Values.service.name}}
data:
  custom.css: |
    header {
        background-color: black;
    }